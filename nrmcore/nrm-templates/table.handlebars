{{#if actions}}<div id="{{id}}-actions" class="table-actions">
{{partial "toolbar"}}</div>{{else}}<div></div>{{/if}}
   <table id="{{id}}" cellpadding="0" cellspacing="0" border="0" class="table table-hover table-striped table-bordered table-condensed" width="100%"{{#unless nolabel}} aria-labelledby="{{id}}-label"{{/unless}}">
      {{#if caption}}<caption class="sr-only">{{caption}}</caption>{{/if}}
   </table>